httpAddr: ":8080"

mysql:
  dsn: "rankflow:rankflow@tcp(127.0.0.1:3306)/rankflow?charset=utf8mb4&parseTime=true&loc=Local"

redis:
  addr: "127.0.0.1:6379"
  password: ""
  db: 0

persistWorkers: 2

auth:
  # Local development stays open by default. Production compose enables auth
  # explicitly and requires both tokens through environment variables.
  enabled: false
  adminToken: ""
  writerToken: ""
